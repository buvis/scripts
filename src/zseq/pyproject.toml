[tool.poetry]
name = "zseq"
version = "0.1.0"
description = "Utilities to work with zettelsequence"
authors = ["Tomáš Bouška <tomas@buvis.net>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
click = "8.3.0"

[build-system]
requires = ["poetry-core", "pynvim"]
build-backend = "poetry.core.masonry.api"
