name: Publish Package to PyPI

on:
  push:
    tags:
      - "*-v*" # e.g. bim-v0.1.0, readerctl-v1.2.3
  workflow_dispatch:
    inputs:
      package:
        description: "Package directory name under src/ (e.g. bim, readerctl)"
        required: true
        type: choice
        options:
          - bim
          - dot
          - fctracker
          - hello_world
          - muc
          - outlookctl
          - pinger
          - readerctl
          - zseq
          - scripts

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          enable-cache: true

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.14.3"

      - name: Determine package
        id: pkg
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "dir=${{ inputs.package }}" >> "$GITHUB_OUTPUT"
          else
            TAG="${GITHUB_REF#refs/tags/}"
            PKG="${TAG%-v*}"
            echo "dir=${PKG}" >> "$GITHUB_OUTPUT"
          fi

      - name: Pin dependencies from lockfile
        if: steps.pkg.outputs.dir != 'scripts'
        run: python dev/pin_deps.py src/${{ steps.pkg.outputs.dir }}

      - name: Build package
        run: uv build src/${{ steps.pkg.outputs.dir }}

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
