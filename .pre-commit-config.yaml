repos:
  - repo: local
    hooks:
      - id: no-dev-sources
        name: block dev mode sources
        entry: bash -c 'if grep -l "tool\.uv\.sources" "$@"; then echo "^^^ dev mode active — run dev/finish-dev first"; exit 1; fi' --
        language: system
        files: (^src/.*/pyproject\.toml$|^bin/[^.]+$)
