[tool.poetry]
name = "scripts"
version = "0.1.0"
description = ""
authors = ["Tomáš Bouška <tomas@buvis.net>"]
readme = "README.md"
packages = [
  { include = "buvis_scripts", from = "src" }
]

[tool.poetry.dependencies]
python = "^3.12"
pyyaml = "^6.0.2"
doogat-core = "^0.1.0"
# doogat-core = {version = "*", source = "test-pypi"}
# doogat-core = {path = "../../doogat/doogat-core", develop = true}
buvis-pybase = "^0.1.2"
# buvis-pybase = {version = "*", source = "test-pypi"}
# buvis-pybase = {path = "../buvis-pybase", develop = true}

[tool.poetry.group.dev.dependencies]
click = "^8.1.7"
pyfiglet = "^1.0.2"

[[tool.poetry.source]]
name = "test-pypi"
url = "https://test.pypi.org/simple/"
priority = "explicit"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
